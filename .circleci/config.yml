stages:
  - stage: Docker initialization
    jobs:
      - job: Installing python dependencies
        steps:
        - command: |
            sudo pip install --upgrade pip
            sudo pip install pytest
            sudo pip install -r requirements.txt  